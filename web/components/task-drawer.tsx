'use client'
import { useQuery } from '@tanstack/react-query'
import { FormEvent, useEffect, useState } from 'react'
import { MessageSquare, X } from 'lucide-react'
import { Comment, request, Task } from '@/lib/api'

export function TaskDrawer({task,token,onClose,onUpdated}:{task:Task;token:string;onClose:()=>void;onUpdated:(task:Task)=>void}){
  const [title,setTitle]=useState(task.title)
  const [description,setDescription]=useState(task.description)
  const [priority,setPriority]=useState(task.priority)
  const [saving,setSaving]=useState(false)
  const [error,setError]=useState('')
  const comments=useQuery({queryKey:['comments',task.id],queryFn:()=>request<Comment[]>(`/api/v1/tasks/${task.id}/comments`,{},token)})
  useEffect(()=>{setTitle(task.title);setDescription(task.description);setPriority(task.priority)},[task])
  async function save(){setSaving(true);setError('');try{const updated=await request<Task>(`/api/v1/tasks/${task.id}`,{method:'PATCH',body:JSON.stringify({version:task.version,title,description,priority})},token);onUpdated(updated)}catch(e){setError(e instanceof Error?e.message:'Could not save task')}finally{setSaving(false)}}
  async function addComment(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=new FormData(form);const body=String(data.get('body')??'').trim();if(!body)return;await request<Comment>(`/api/v1/tasks/${task.id}/comments`,{method:'POST',body:JSON.stringify({body})},token);form.reset();await comments.refetch()}
  return <div className="fixed inset-0 z-50 flex justify-end bg-black/30" onMouseDown={onClose}><aside className="h-full w-full max-w-2xl overflow-y-auto border-l border-[var(--line)] bg-[var(--panel)] p-5 sm:p-7" onMouseDown={e=>e.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-medium muted">{task.identifier}</p><input value={title} onChange={e=>setTitle(e.target.value)} className="mt-1 w-full bg-transparent text-2xl font-semibold tracking-tight outline-none" aria-label="Task title"/></div><button onClick={onClose} className="rounded-xl border border-[var(--line)] p-2" aria-label="Close task"><X size={18}/></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Priority<select value={priority} onChange={e=>setPriority(e.target.value as Task['priority'])} className="mt-2 w-full rounded-xl border border-[var(--line)] bg-[var(--panel)] px-3 py-2.5">{['urgent','high','medium','low','none'].map(p=><option key={p} value={p}>{p[0].toUpperCase()+p.slice(1)}</option>)}</select></label><div className="text-sm"><p className="font-medium">Status</p><p className="mt-2 rounded-xl border border-[var(--line)] px-3 py-2.5 muted">{task.status.replace('_',' ')}</p></div></div><label className="mt-6 block text-sm font-medium">Description<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={10} placeholder="Add context, acceptance criteria, links, or notes…" className="mt-2 w-full resize-y rounded-2xl border border-[var(--line)] bg-transparent p-4 leading-6 outline-none focus:border-indigo-500"/></label>{error&&<p className="mt-3 rounded-xl bg-red-500/10 p-3 text-sm text-red-600">{error}</p>}<div className="mt-4 flex justify-end"><button disabled={saving} onClick={()=>void save()} className="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{saving?'Saving…':'Save changes'}</button></div><div className="mt-10 border-t border-[var(--line)] pt-6"><h2 className="flex items-center gap-2 font-semibold"><MessageSquare size={18}/>Discussion</h2><form onSubmit={addComment} className="mt-4"><textarea name="body" required rows={3} placeholder="Write a comment…" className="w-full rounded-2xl border border-[var(--line)] bg-transparent p-3 outline-none"/><div className="mt-2 flex justify-end"><button className="rounded-xl border border-[var(--line)] px-4 py-2 text-sm font-semibold">Comment</button></div></form><div className="mt-5 space-y-3">{comments.data?.map(comment=><article key={comment.id} className="rounded-2xl bg-black/[.035] p-4 dark:bg-white/[.035]"><div className="mb-2 flex items-center justify-between gap-3"><span className="text-xs font-medium muted">{comment.author_id.slice(0,8)}</span><time className="text-xs muted">{new Date(comment.created_at).toLocaleString()}</time></div><p className="whitespace-pre-wrap text-sm leading-6">{comment.body}</p></article>)}{comments.isLoading&&<p className="text-sm muted">Loading comments…</p>}{comments.data?.length===0&&<p className="text-sm muted">No comments yet.</p>}</div></div></aside></div>
}
